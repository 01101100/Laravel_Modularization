# Selling Wears - ST_VN15

## Install Guide
1. `composer install`
2. `cp .env.example .env`
3. `php artisan key:generate`
4. Sửa các trường `DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD` trong file `.env`
5. `php artisan serve` website will be start at `localhost:8000`

## Testing
